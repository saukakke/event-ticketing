{
  "name": "event-ticketing",
  "private": true,
  "version": "1.0.0",
  "description": "3MTT Capstone full-stack event ticketing platform",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "start": "concurrently -k \"npm run start --workspace frontend\" \"npm run start --workspace backend\"",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "db:generate": "npm run db:generate --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "db:seed": "npm run db:seed --workspace backend",
    "db:studio": "npm run db:studio --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
