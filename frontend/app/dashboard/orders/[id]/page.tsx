"use client";
import {useEffect,useState} from "react"; import {api,formatDate,formatNaira} from "@/lib/api";
export default function OrderDetail({params}:{params:Promise<{id:string}>}){const[o,setO]=useState<any>();const[e,setE]=useState("");useEffect(()=>{params.then(p=>api<any>(`/orders/me/${p.id}`).then(setO).catch(x=>setE(x.message)))},[params]);if(e)return <main className="container section"><div className="empty"><h2>Unable to load order</h2><p>{e}</p></div></main>;if(!o)return <main className="container section"><p>Loading order…</p></main>;return <main className="container section"><div className="eyebrow">Order</div><h1>{o.id}</h1><p className="meta">{o.event.title} · {formatDate(o.createdAt)}</p><div className="stats"><div className="stat"><span className="meta">Status</span><strong>{o.status}</strong></div><div className="stat"><span className="meta">Total</span><strong>{formatNaira(o.totalKobo)}</strong></div><div className="stat"><span className="meta">Payment reference</span><strong>{o.paymentReference||"Pending"}</strong></div></div><h2 style={{marginTop:"2rem"}}>Tickets</h2><div className="table-wrap"><table><thead><tr><th>Code</th><th>Ticket type</th><th>Status</th><th>Check-in</th></tr></thead><tbody>{o.tickets.map((t:any)=><tr key={t.id}><td>{t.code}</td><td>{o.items.find((i:any)=>i.ticketTypeId===t.ticketTypeId)?.ticketType?.name||"Ticket"}</td><td>{t.status||"ACTIVE"}</td><td>{t.checkedIn?formatDate(t.checkedInAt):"Not checked in"}</td></tr>)}</tbody></table></div></main>}
