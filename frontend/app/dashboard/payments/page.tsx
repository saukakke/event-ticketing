"use client";
import {useEffect,useState} from "react"; import {api,formatDate,formatNaira} from "@/lib/api";
export default function Payments(){const[p,setP]=useState<any[]>([]);const[e,setE]=useState("");useEffect(()=>{api<any[]>("/payments/me").then(setP).catch(x=>setE(x.message))},[]);if(e)return <main className="container section"><div className="empty"><h2>Unable to load payments</h2><p>{e}</p></div></main>;return <main className="container section"><div className="eyebrow">My account</div><h1>Payment history</h1><div className="table-wrap" style={{marginTop:"1.5rem"}}><table><thead><tr><th>Reference</th><th>Event</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead><tbody>{p.map(x=><tr key={x.id}><td>{x.paymentReference}</td><td>{x.event.title}</td><td>{formatNaira(x.totalKobo)}</td><td>{x.status}</td><td>{formatDate(x.createdAt)}</td></tr>)}</tbody></table></div></main>}
